import { StorageAPIWrapperService } from './storage-api-wrapper.service';

describe('StorageAPIWrapper', () => {
  it('should create an instance', () => {
    expect(new StorageAPIWrapperService()).toBeTruthy();
  });
});
